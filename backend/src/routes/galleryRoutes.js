// const express = require('express');
// const router = express.Router();
// const authenticate = require('../middleware/authMiddleware');

// // Rotas da galeria (exemplo)
// router.post('/add', authenticate, (req, res) => {
//   // Lógica para adicionar um novo membro ou projeto
//   res.status(200).json({ message: 'Projeto adicionado com sucesso' });
// });

// router.get('/', authenticate, (req, res) => {
//   // Lógica para listar os membros ou projetos
//   res.status(200).json({ message: 'Galeria de membros/projetos' });
// });

// module.exports = router;
